services:
  contabilidad:
    build: .
    container_name: contabilidad-app
    ports:
      - "8000:8000"
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/contabilidad.db
    restart: unless-stopped

volumes:
  db-data:
